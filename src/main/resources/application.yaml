spring:
  application:
    name: music-store
  data:
    mongodb:
      uri: mongodb://localhost:27017/music-store-db
      connection-timeout: 30000
      socket-timeout: 30000
      retry-writes: true
      max-wait-time: 60000
  retry:
    max-attmepts: 5
    delay: 2